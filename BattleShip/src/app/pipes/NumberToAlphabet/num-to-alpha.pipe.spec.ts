import { NumToAlphaPipe } from './num-to-alpha.pipe';

describe('NumToAlphaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumToAlphaPipe();
    expect(pipe).toBeTruthy();
  });
});
